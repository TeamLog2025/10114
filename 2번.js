const path = require( 'path');
const filePath = `/Users/student/project/test.js`;
console.log( '디렉터리명:', path.dirname(filePath));
console.Log('파일명:', path.basename(fttePath));
console.log('확장자:', path.extname(filePath));
